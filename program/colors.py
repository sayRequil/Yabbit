def colorss(color):
  if color == "preset": # (The functions that came with, like Entry)
    return "blue"
    
  if color == "string": # (A string that has quotes, like this: "Hello World")
    return "purple"
